Al consultar la ruta GET /reportes, el servidor devuelve la lista de reportes almacenados en memoria. Cada vez que se registra un reporte mediante la ruta POST, este se agrega al array reportes y puede visualizarse posteriormente usando GET /reportes. Si el servidor se reinicia, la lista vuelve a quedar vacía porque los datos no se almacenan en una base de datos.